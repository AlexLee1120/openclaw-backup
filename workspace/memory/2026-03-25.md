# 2026-03-25 - 數位 CTO 小艾 日誌

## 今日成果回報 (08:06 - 08:15)

- **Alibaba Cloud Model Studio (Qwen) 整合：**
  - **API Key 部署：** 完成 `sk-e5...` Key 的安全儲存 (`~/.openclaw/credentials/modelstudio.json`)。
  - **系統配置：** 啟用 `modelstudio` 插件，建立 `qwen` 模型別名 (指向 `modelstudio/qwen-plus`)。
- **Anthropic (Claude) 整合 (第二備援)：**
  - **API Key 部署：** 完成 `sk-ant-api03...` Key 的安全儲存 (`~/.openclaw/credentials/anthropic.json`)。
  - **系統配置：** 啟用 `anthropic` 插件，建立 `claude` 模型別名 (指向 `anthropic/claude-3-5-sonnet-latest`)。
  - **備援回報規則：** 若切換至 Claude，將立即回報前兩組金鑰失效並保持過程安靜。
- **自動化路由設計：**
  - **主用 (Gemini)：** 複雜任務。
  - **第一備援/簡單 (Qwen)：** 簡單任務。
  - **第二備援 (Claude)：** 最後防線。
  - **規範同步：** 更新 `SOUL.md` 與 `AGENTS.md` 納入此路由策略。

- **上下文清理與記憶固化：**
  - 應 Alex 要求清理當前會話上下文，並將關鍵技術配置與決策固化至長期記憶。

## 決策與變動
- **模型路由原則：** 確立「價值驅動」的模型使用策略，確保在高產出的同時優化 Token 消耗。

## 下一步計畫
- 持續觀察 Qwen 在簡單任務中的表現，並根據實際配額情況動態調整權重。

## 今日更新 (10:11)
- **移除 Qwen 備援：** 由於 Alibaba Cloud Model Studio 的 API 金鑰已失效 (401 invalid access token)，應 Alex 指示已將 Qwen 從系統配置與模型清單中完全移除。目前依賴 Gemini 3.1 Pro Preview (主用) 與 Claude Sonnet 4.5 (備援)。
Alex 在 Web UI 試圖回覆時遇到了 429 錯誤，訊息未送達。

### 14:16 (Asia/Taipei)
- 處理 Gemini API `429 RESOURCE_EXHAUSTED` (Rate Limit) 問題。
- 將全域主模型切換為 `google/gemini-3.1-pro-preview`，以穩定配額並提升處理能力。
- 確立跨模型備援機制，將 `anthropic/claude-sonnet-4-5` 加入全域 `fallbacks` 設定檔。
- 從配置與憑證檔中移除已過期的 Alibaba Cloud Model Studio (Qwen) 金鑰。
- 確認 `google:default` 與 `google:backup` 雙層金鑰配置生效，並成功觸發同模型金鑰輪替。
- 協助排查網頁版後台 (Control UI) 指令中斷狀況：發現使用者傳送的指令在前端即遭遇 429 Rate Limit 錯誤而被攔截，未成功寫入系統對話紀錄，導致系統呈「待機」狀態。已向使用者說明並引導從 Telegram 接續對話。
- 向 Alex 報告 `main` 其實已於 13:45 完成了【選項 A（網頁實裝）】與【選項 B】，只是因為 Web UI 斷線未收到回覆。確認 `app.html` 已經部署上線至 GitHub。
- 已導入並部署 SlowMist OpenClaw Security Practice Guide v2.8 強化防禦基準。包含：寫入零信任記憶、鎖定設定權限、生成哈希基線，並部署 `nightly-security-audit.sh` 夜間自動巡檢。
- 引導 Alex 建立 GitHub/GitLab 私有儲存庫並產生 Personal Access Token (PAT)，以完成大腦災備系統的最後一哩路。
